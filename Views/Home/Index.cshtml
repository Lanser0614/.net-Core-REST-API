@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @foreach (var item in Model)
    {
        <h1 class="display-4">@item.Name</h1>
        <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">@item.Email</a>.</p>
    }
</div>